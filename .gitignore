# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# Lumen local workspace data
.agents/
.lumen/
.claude/
.gitnexus/
.stfolder/
.stignore

# Packaged release artefacts produced by `flutter build windows --release`
# + the staging step in dist/. Regeneratable; never commit binaries.
dist/

# Throwaway preview output from `tools/extract_logo_no_bg.dart` and any
# future tooling-side debug renders. The script writes a dark-glass
# composite of the in-app logo here so an operator can sanity-check
# the alpha-keying before committing the asset; not a shipped file.
tools/_out/

# Secrets and local environment
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key
*.p12

# Local Remote Access bearer token, written by tool/remote_pair.dart
# during dev smoke. Treat as a secret — anyone with this token can
# drive the desktop chat over the LAN/Tailscale bind.
tool/.lumen_remote_token
*.pfx
credentials.json
secrets.json

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/
/coverage/
website/.next/
website/node_modules/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android build and local config
/android/.gradle/
/android/local.properties
/android/**/build/

# iOS / macOS generated dependencies and local Xcode state
/ios/Pods/
/ios/.symlinks/
/ios/Flutter/App.framework
/ios/Flutter/Flutter.framework
/ios/Flutter/Flutter.podspec
/ios/Flutter/Generated.xcconfig
/ios/Flutter/ephemeral/
/ios/Runner.xcworkspace/xcuserdata/
/ios/Runner.xcodeproj/xcuserdata/
/macos/Pods/
/macos/.symlinks/
/macos/Flutter/ephemeral/
/macos/Flutter/GeneratedPluginRegistrant.swift
/macos/Runner.xcworkspace/xcuserdata/
/macos/Runner.xcodeproj/xcuserdata/

# Linux / Windows generated Flutter intermediates
/linux/flutter/ephemeral/
/windows/flutter/ephemeral/

# Native build products
*.o
*.obj
*.dll
*.exe
*.exp
*.lib
*.pdb
*.ilk
*.so
*.dylib

# Vendored binaries we deliberately commit. ripgrep ships in the
# Flutter asset bundle so SEARCH_TEXT works for colleagues without
# requiring a manual `winget install BurntSushi.ripgrep.MSVC` /
# `brew install ripgrep`. See lib/services/ripgrep_provisioner.dart
# for the runtime-extraction flow.
!assets/bin/**

# OS junk
Thumbs.db
Desktop.ini

# Python scratch/cache files if helper scripts are added
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# AGENTS.md and CLAUDE.md are intentionally tracked — they hold the
# GitNexus integration block (managed by `npx gitnexus analyze`) plus
# any project-wide agent rules. Ignoring them would leave fresh
# clones / new collaborators without GitNexus context until they
# re-ran the installer. Don't add them here without a concrete
# reason; they're meant to ship with the repo.

# Defensive: SQLite scratch / journal files if any future feature
# starts caching to disk. Today nothing writes these; keeping the
# rule means a future addition won't accidentally commit a half-
# written DB.
*.sqlite
*.sqlite-journal
*.db
CLAUDE.md
AGENTS.md

# FVM Version Cache. Ignore the cached SDK binaries / symlink (huge,
# per-machine) but COMMIT the pin file (.fvm/fvm_config.json + the
# .fvm/version / .fvm/release sidecars). That way `fvm use` after a
# fresh clone resolves to the project-pinned Flutter automatically.
# The project intentionally builds against Flutter 3.41.x (Dart 3.11)
# because `re_editor 0.8.0` (the core editor widget) does NOT compile
# on Flutter 3.44+ / Dart 3.12 — `_CodeInputController` is missing
# overrides for new TextEditingController members. Bumping the pin
# requires either a newer re_editor (none published) or a fork/patch.
.fvm/flutter_sdk
.fvm/versions/